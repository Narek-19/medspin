import React from 'react';
import * as styles from './style.module.css';

export const MedspinButton = () => {
    return(
        <button className = {styles.Button}>
            <span>ReadMore</span>
        </button>
    )
}